const FormHeading = (headingText) => {
  const heading = document.createElement('h3');
  heading.textContent = headingText;
  return heading;
};

const Accordian = (foo, bar) => {};

export { FormHeading, Accordian };
